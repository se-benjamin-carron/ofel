using System;

namespace Ofel.Infrastructure
{
    public static class Program
    {
        // Minimal entrypoint to satisfy the compiler for GUI project during CI/build.
        // The real application entry (Avalonia App) can replace this later.
        public static int Main(string[] args)
        {
            Console.WriteLine("Ofel.Infrastructure stub Main - build succeeded.");
            return 0;
        }
    }
}
